spring.application.name=ems-backend
server.port=${PORT:8080}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always
spring.jpa.show-sql=false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:EMSNew}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:1234}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.data.web.pageable.default-page-size=10

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:hrtektalis@gmail.com}
spring.mail.password=${MAIL_PASSWORD:hdmc jkdk krvq fiok}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

ems.jwt.secret=${JWT_SECRET:Z3V2d3dxd3Vpd2V1d2V1aXdlcXVpd2V1cXVpd2V1cXVpd2V1}
ems.jwt.expiration=900000
ems.jwt.refreshExpiration=8640000000

logging.level.root=WARN
logging.level.com.employee=INFO
logging.level.org.springframework.security=WARN

cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:3000}
